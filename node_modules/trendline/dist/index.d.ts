declare function createTrend(data: Array<{
    [key: string]: number;
}>, xKey: string, yKey: string): {
    slope: number;
    yStart: number;
    calcY: (x: number) => number;
};

export { createTrend, createTrend as default };
